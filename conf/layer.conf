BBFILES += "${LAYERDIR}/recipes/*/*.bb ${LAYERDIR}/recipes/*/*.bbappend ${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"
BBPATH .= ":${LAYERDIR}"
BBFILE_COLLECTIONS += "sepolicy"
BBFILE_PRIORITY_sepolicy = "11"
BBFILE_PATTERN_sepolicy := "^${LAYERDIR}/"

PREFERRED_PROVIDER_virtual/refpolicy = "refpolicy-mls"
PREFERRED_VERSION_refpolicy-mls = "2.20190201"
SELINUX_FILE_CONTEXTS ?= "${IMAGE_ROOTFS}/etc/selinux/mls/contexts/files/file_contexts"

# meta-selinux requires pam if selinux is in DISTRO_FEATURES
DISTRO_FEATURES_BACKFILL_append = " ${@bb.utils.contains('DISTRO_FEATURES', 'selinux', 'pam', '', d)}"

LAYERSERIES_COMPAT_sepolicy = "${LAYERSERIES_COMPAT_core}"
